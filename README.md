# Node Course Workspace

This repository contains lesson folders and exercises for the Node.js course.

## Structure

- Each numbered folder corresponds to a lesson or practice module.
- Individual lesson folders may contain their own `package.json` files.

## Usage

- Open a specific lesson folder and run its scripts as needed.
- Install dependencies per lesson:
  - `npm install`
  - `npm run <script>`
